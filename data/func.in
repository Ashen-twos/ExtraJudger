#include <stdio.h>

int g_AAA = 0;

int init()
{
    a++;b++;c++;
    printf("%d\n",a);;;;;;;;;
}
int main()
{
    sort();
    int nums[10];
    int *ptr;
    ptr = (int*)malloc(1023);
    int myAns = 0;
    //init();
    lower_bound();
    int x, y; ; ; ;
    int asdasFd;
    scanf("%d%d", &x, &y);;;;;;;
    printf("%d", x+y);
    free(ptr);
}