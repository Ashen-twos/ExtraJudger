char* global_itertr;
iter = (char*) malloc(sizeof(int)*1024);
int asssole();

int addum()
{
    scanf("%d",&a);
}

int main() 
{
    int ans = 0;
    int maxum[1024];
    for(int i=0; i<n; i++)
        for(int i=0; i<n; i++)
            for(int i=0; i<n; i++)
                myAns++;
    printf("%d\n", 1);
    free(ptr);
}

free(iter);
int farar()
{

}

